## This folder is for parsing the vulkan xml file to enable faster engine development 

dependent on [Vulkan-Docs](https://github.com/KhronosGroup/Vulkan-Docs/) for its written parsing of the vk.xml file specification. see `/include/Vulkan-Docs` for its source code